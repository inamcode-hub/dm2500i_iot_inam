// network/ws/scheduler/index.js
// Manages scheduled background jobs

function startAllJobs() {
  // Currently no scheduled jobs
  // Future jobs: OTA updates, token refresh, etc.
}

function stopAllJobs() {
  // Stop any running scheduled jobs
}

module.exports = { startAllJobs, stopAllJobs };
